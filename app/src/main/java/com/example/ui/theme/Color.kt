package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Premium Slate & Climate Color Palette
val SlateDark = Color(0xFF0F172A)     // Tailwind Slate 900 (Main deep space background)
val SlateMedium = Color(0xFF1E293B)   // Tailwind Slate 800 (Card & UI surfaces)
val SlateLight = Color(0xFF334155)    // Tailwind Slate 700 (Light highlight slate)
val TextPrimary = Color(0xFFF8FAFC)   // Slate 50 (Very bright white-gray for main text)
val TextSecondary = Color(0xFF94A3B8) // Slate 400 (Muted gray for subtitles)

// Climate Tool Accent Colors
val ClimatePrimary = Color(0xFF0EA5E9)   // Bright Sky Cyan (Primary controls)
val ClimateSecondary = Color(0xFFF59E0B) // Warn Amber (Secondary alerts / anomaly indexes)

val TempWarm = Color(0xFFEF4444)      // Temperature Crimson
val TempCold = Color(0xFF3B82F6)      // Temperature Cobalt
val WindCyan = Color(0xFF22D3EE)      // Moving Wind Stream Cyan
val RainBlue = Color(0xFF60A5FA)      // Rain Particle Light Blue
val CloudWhite = Color(0xFFE2E8F0)    // Muted Cloud White/Gray
val HumidityTeal = Color(0xFF34D399)  // Humidity Greenish Teal

val BackgroundDark = Color(0xFF0B0F19) // Extra deep canvas for maximum overlay contrast
val CardSlate = Color(0xFF151D30)      // Highly cohesive dark card surface
val BorderSlate = Color(0xFF24324F)    // Card separators and subtle borders

